<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory
        android:title="Movies Settings"
        >

    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="@string/pref_sorting_key"
        android:title="@string/pref_sort_title"
        android:defaultValue="@string/sort_default"
        android:entries="@array/pref_list_sort"
        android:entryValues="@array/pref_sort_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    </PreferenceCategory>

</PreferenceScreen>
